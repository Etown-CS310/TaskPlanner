import {Modal, View, Text, StyleSheet} from 'react-native';

function RepeatingModal(){

    return (
        ''
    )
}

export default RepeatingModal;

const styles = StyleSheet.create({
    modalContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'rgba(0, 0, 0, 0.5)',
    },
});